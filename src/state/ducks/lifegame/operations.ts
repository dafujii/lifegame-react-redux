import actions from "./actions";

export const next = actions.next;
export const startAutoPlay = actions.startAutoPlay;
export const stopAutoPlay = actions.stopAutoPlay;

export default {
  next,
  startAutoPlay,
  stopAutoPlay
};
